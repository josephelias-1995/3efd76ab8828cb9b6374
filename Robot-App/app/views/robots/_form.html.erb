<%= form_with(model: robot, local: true) do |form| %>
  <% if robot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(robot.errors.count, "error") %> prohibited this robot from being saved:</h2>

      <ul>
        <% robot.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :table_cordinate %>
    <%= form.text_field :table_cordinate %>
  </div>

  <div class="field">
    <%= form.label :is_placed %>
    <%= form.check_box :is_placed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
